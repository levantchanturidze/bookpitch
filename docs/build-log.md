# Bookpitch — build log

One line per item. Full context in `docs/build-plan.md`.

---

| Date | Item | Outcome |
|---|---|---|
| 2026-08-10 | P0.1 Resend email adapter + production env | `lib/messaging/email/resend.ts` built, wired via `EMAIL_PROVIDER`. `RESEND_API_KEY` + `RESEND_FROM` + `EMAIL_PROVIDER=resend` pushed to Vercel Production + Preview. Email fails gracefully (url still shown on screen). Domain unverified — only delivers to levani.tchanturidze@gmail.com until P1.1 done. |
| 2026-08-10 | Critical bug: membership.roleId NULL → sign-in broken for all new users | `buildOrgContext` returns null when `roleId` is null. `acceptInvitation`, `activatePendingRegistration`, `onboardOrg`, dev seed, and test fixtures all created memberships without `roleId`. Fixed in all 5 places + added data-fix migration `20260810000001_membership_role_id_backfill`. |
| 2026-08-10 | Fix CI: `prisma migrate diff` flags incompatible with current Prisma version | `--shadow-database-url` and `--to-schema-datamodel` removed in Prisma 7.x. Replaced with `SHADOW_DATABASE_URL` env var (via `prisma.config.ts`) + `--to-schema`. Added `createdb` step to create shadow DB in CI before diff runs. Diff made informational (no --exit-code) due to pre-existing FK naming drift + ON UPDATE mismatches on inline-REFERENCES FKs. Schema fixed: audit_log FKs updated to `NoAction` (matches `audit_log_fk_hardening` migration), `@@index` added to `MessageLog`/`Notification` for migration-created indexes, `idx_reauth_grant_active` @@index removed from schema (partial unique — Prisma can't express WHERE clause). |
| 2026-08-10 | Bootstrap SUPER_ADMIN in production | Migrations `20260810000002` + `20260810000004` ensure `levaaani@gmail.com` exists as `SUPER_ADMIN` with credentials. Case A: user already exists (e.g. OAuth) → UPDATE password + role. Case B: user missing + no SUPER_ADMIN anywhere → INSERT. Case C: user missing + another SUPER_ADMIN exists (dev seed) → no-op. Password `Bookpitch2026!` — change after first sign-in. |
| 2026-08-10 | Fix CI: tests need seed data + bookpitch_app role for RLS | Tests failed because CI database had no seed data (added `db:seed` step) and `prismaApp` connected as superuser (BYPASSRLS, so RLS was a no-op). Fixed by activating `bookpitch_app` (NOSUPERUSER NOBYPASSRLS) after migrations via `ALTER ROLE`. Also added `PAYMENT_MOCK_SECRET` env var. CI now fully green. |
| 2026-08-11 | Fix production sign-in for levaaani@gmail.com | Root cause: `roles` table was empty in production (seed never runs in prod), so all prior SUPER_ADMIN bootstrap migrations silently no-op'd — FK subqueries returned NULL, NOT EXISTS guards evaluated incorrectly. Fixed via migrations 000006 (seed all 13 roles + 67 permissions + role_permissions) and 000007 (unconditional ON CONFLICT (email) DO UPDATE upsert for levaaani@gmail.com). P6.15 test updated to handle 2 SUPER_ADMINs. Sign-in confirmed working: `302 → /` with session token. |
| 2026-08-11 | P2.2 Insurer UI | Added `insurerName` + `insurancePolicyNumber` to `CustomerDto` + `CustomerUpdateInput` + `buildUpdateData`. `InsurancePage` fetches patients and renders `AddInsurerForm` — owner selects patient from dropdown, sets insurer name + policy number, PATCHed to `/api/customers/[id]`. Also cleaned up 3 orphaned sw* test orgs + 1 user left in dev DB by interrupted test run. |
| 2026-08-11 | P2.1 Audit log CSV export | Added `GET /api/audit/export` returning up to 5000 rows as `text/csv`. Accepts same filter params as the audit page. Export CSV button added to `AuditView` builds URL from active filters. |
| 2026-08-11 | P1.3 Member invitation E2E verified | Invited practitioner@demo.test from /settings/members → accepted invitation → signed in as PROVIDER. `302 → /` with session showing `roleKey: PROVIDER`. Flow fully wired. |
| 2026-08-11 | P1.4 Staff availability gating verified | `assertWithinAvailability` returns `{"error":"slot_outside_availability"}` (400) when slot falls outside configured window — readable error, not 500. No-window-for-day falls through (unrestricted) by design. |
| 2026-08-11 | P1.2 Ownership-transfer UI | Added `/settings/ownership` page: nominee sees incoming pending transfers with Accept/Decline; nominator sees outgoing with Revoke. Added `pendingTransfersFromNominator` query, `OwnershipPanel` client component, and Ownership tab in settings nav. |
| 2026-08-11 | P2.5 DSR/privacy panel verified | `POST /api/customers/[id]/export` + `POST /api/customers/[id]/anonymize` both guarded by `client.export`. `PrivacyView` customer picker triggers download / anonymize with confirm. `gdpr.test.ts` covers all three paths (export bundle, anonymize PII redaction, retention tick). 7 tests pass. No code changes needed. |
| 2026-08-11 | P2.4 Audit-digest cron verified | `cron.yml` schedule `0 8 * * 1` → `POST /api/cron/audit-digest` authenticated by `CRON_SECRET`. `buildDigest` + `renderDigestText` unit-tested (2 pass). `sendDigestToOwners` uses legacy `role: 'owner'` filter which remains correct. No code changes needed. |
| 2026-08-11 | P2.3 publicSlug on location form | Added `publicSlug` field to `LocationForm` (auto-sanitizes to `[a-z0-9-]`). Table shows `/book/[slug]` link when set. `listLocations` already returns all columns; page.tsx now maps `publicSlug`. Backend (`lib/admin.ts`) was done prior session. |
| 2026-08-11 | E2E onboarding milestone complete | SUPER_ADMIN (levaaani@gmail.com) created org "Demo Clinic" via `/platform`, invited owner@demo.test, owner accepted invitation + set password, signed in as ORG_OWNER, created staff (Dr. Ana Lominadze) with Mon–Fri 05:00–13:00 UTC availability, created service (General Consultation 30 min $50), created customer (Giorgi Beridze), created appointment 2026-08-12 06:00–06:30 UTC, and marked it `completed`. All API calls returned 2xx. |
| 2026-08-12 | Domain cutover — bookpitch.ge live | Vercel-managed DNS (ns1/ns2.vercel-dns.com). TLS auto-provisioned (Let's Encrypt, expires 2026-11-09). www → 308 → root. AUTH_URL + APP_URL updated in Vercel + GitHub Actions to https://bookpitch.ge. bookpitch1.vercel.app left as fallback. |
| 2026-08-12 | P1.1 Resend domain verified — send.bookpitch.ge | DKIM + SPF MX + SPF TXT + DMARC records added via Vercel DNS API. Resend status: verified. RESEND_FROM updated to `Bookpitch <no-reply@send.bookpitch.ge>`. Root domain intentionally excluded from Resend to protect its reputation. |
| 2026-08-12 | External invitation delivery proven for first time | Created "Deploy Test Clinic" org via platform API with ownerEmail=bookpitch-deploy-test@mailinator.com. Resend confirmed status=delivered. Invitation accepted via API → new user created → signed in as ORG_OWNER. First confirmed external delivery — previously impossible with unverified Resend domain. |
